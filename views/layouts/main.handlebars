<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Tech Blog</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
  </head>

  <body>
    <div class="wrapper">
      <header>
        <div>
          <nav>
            <div class="navbar-buttons">
              <a class="homepage-button-homepage" href="/">Home</a>
              <a class="dashboard-button-homepage" href="/dashboard">Dashboard</a>
              {{#if loggedIn}}
                <button id="logout" class="btn-no-style">Logout</button>
              {{else}}
                <a class="login-button-homepage" href="/login">Login</a>
              {{/if}}
            </div>
          </nav>
          <div>
          <div class="title-box">
            <h1><a class="navbar-title" href="/">Tech Blog</a></h1>
          </div>
          </div>
        </div>
      </header>
      <main>
        {{{body}}}
      </main>
      <footer>
        Thanks for visiting!
      </footer>
    </div>
    {{#if loggedIn}}
      <script src="/javascript/logout.js"></script>
    {{/if}}
  </body>

</html>